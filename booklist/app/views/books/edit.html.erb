<h2 style="color: #AB6BE0;">Edit Book</h2>

<%= form_for(@book) do |f| %>
	<%= f.label :title %><%= f.text_field :title %>
	<%= f.label :description %><%= f.text_area :description %>
	<%= f.label :isbn %><%= f.text_field :isbn, min: 1 %>

	<%= f.submit %>
<% end %>
